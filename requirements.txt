# MINERVA requirements (Colab-tested, Python 3.11)

# Core DS stack
numpy<2
pandas
scikit-learn
tqdm
joblib

# Hugging Face stack (pin to stable 4.x to avoid breaking API changes)
transformers==4.33.3
datasets==2.14.7
tokenizers==0.13.3
accelerate==0.23.0
evaluate>=0.4.0
huggingface-hub<1.0.0

# NLP utilities
sentencepiece
safetensors
protobuf<5

# Interpretability (optional but used in MINERVA explainability extensions)
captum>=0.7.0

# Qlattice / symbolic regression
feyn

# Plotting / reporting
matplotlib

# API layer (optional)
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic<3
